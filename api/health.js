module.exports=(req,res)=>res.status(200).json({
  ok:true,
  app:'TurnoSmart',
  version:'61.0.0',
  native_ios_picker_enabled:true,
  real_media_buttons_enabled:true,
  overlay_input_removed:true,
  sgman_configured:Boolean(process.env.SGMAN_TOKEN)
});
